server:
  port: 8070

spring:
  datasource:
    url: jdbc:mysql://xxxxxxxx:3306/test_db?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: xxxx.
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  mapper-locations: classpath*:mapper/**/*.xml
  configuration:
    mapUnderscoreToCamelCase: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl # 控制台打印 SQL

# JWT 配置
jwt:
  secret: gP#73YvUq!4rEwzX1@cN9kDf
  expiration: 24    # 访问令牌过期时间（小时）
  refresh: 168      # 刷新令牌过期时间（小时，7天）
  token-prefix: "Bearer "
  header: "Authorization"

